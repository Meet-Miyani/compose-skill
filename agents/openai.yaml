interface:
  display_name: "Compose"
  short_description: "Architecture and best practices for Compose apps"
  default_prompt: "Use $compose to build, refactor, or review a Jetpack Compose or Compose Multiplatform feature with clean architecture, state modeling, and performance best practices."
